.footer
  .footer__contact
    h3.footer__title Let's have a chat!
    each anchor in data.footer.contact
      a.footer__link(
        class=`bb-${anchor.icon}`
        title=`${anchor.label}`
        href=`${anchor.url}`
      )= anchor.label
